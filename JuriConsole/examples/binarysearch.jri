# not working


fun search :l target
	len = ?:l
	len2 = len // 2
	l= len//2:l
	if l == target
		return 1
	if len < 2
		return 0
	if len2:l > target
		:l1 = init len2 0
		i = 0
		if i < len2 repeat
			i:l1 = i:l
			i=i+1
			
		
		return search(:l1 target)
	
	:l2 = init len2 0
	i = len2
		
	if i < ?:l repeat
		(i-len2):l2 = i:l
		i=i+1
			
	return search(:l2 target)

:arr = [1 2 4 5 6 7 8 9 10 12]

search(:arr target)


